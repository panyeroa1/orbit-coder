{
  "name": "eburon-coder-desktop",
  "version": "0.0.0-dev",
  "description": "Lightweight desktop app for Eburon Coder",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:dmg": "tauri build --target universal-apple-darwin",
    "build:linux": "tauri build --bundles deb,rpm"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
